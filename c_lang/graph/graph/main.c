#include <stdio.h>

#define N 100

int main()
{
    int n = 0;
    int
    int graph[N][N];

    scanf("%d", &n);

    for(int i = 0; i < n; ++i){
        for(int j = 0; j < n; ++j){
            graph[i][j] = 0;
        }
    }

    for(int i = 0; i < n; ++i){
        scanf();
    }
    return 0;
}